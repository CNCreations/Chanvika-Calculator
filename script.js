const search=document.getElementById("search");const cards=[...document.querySelectorAll(".card")];const noResults=document.getElementById("noResults");if(search){search.addEventListener("input",()=>{const q=search.value.trim().toLowerCase();let visible=0;cards.forEach(card=>{const match=card.dataset.name.includes(q);card.hidden=!match;if(match)visible++});document.querySelectorAll(".category-section").forEach(s=>{const any=[...s.querySelectorAll(".card")].some(c=>!c.hidden);s.hidden=!any});if(noResults)noResults.hidden=visible!==0})}